name: 'CI/CD'

on:
  push:
    branches: 
      - "master"

jobs:
  hello-world:
    runs-on:
      - ubuntu-latest

    steps:
      - name: "Print hello world"
        run: |
          echo hello world
          echo barev gugush
          echo barev dzez